[package]
name = "heroku-registry"
version = "0.1.0"
edition = "2024"
rust-version = "1.89"
description = "Heroku command registry and schema management"
license = "MIT OR Apache-2.0"
authors = ["Heroku CLI Team <devnull@example.com>"]
repository = "https://example.com/heroku"

[dependencies]
anyhow = { workspace = true }
bincode = { workspace = true }
serde = { workspace = true }
serde_json = { workspace = true }
clap = { workspace = true }
heroku-types = { path = "../types" }
heroku-util = {path = "../util"}

[build-dependencies]
anyhow = { workspace = true }
bincode = { workspace = true }
serde_json = { workspace = true }
heroku-registry-gen = { path = "../registry-gen" }
heroku-types = { path = "../types" }
